@{
    # These are strict identity families, not merely similar exercises. Every
    # member demonstrates the same fundamental movement even when one catalog
    # record is a hold, repetition, or naming variant. A workout may select at
    # most one member of a family as a base muscle-group slot.
    Families = @{
        '104' = @(104, 136, 626) # Sumo / wide turned-out squat
        '113' = @(113, 135)      # Overhead squat
        '115' = @(115, 997)      # Full pistol squat
        '117' = @(117, 123)      # Lateral lunge
        '120' = @(120, 184)      # Split squat
        '124' = @(124, 636)      # Curtsy lunge with side crunch
        '125' = @(125, 973)      # Reverse lunge with torso twist
        '159' = @(159, 649)      # Bent-knee curl to diagonal abduction
        '177' = @(177, 186)      # Hip airplane
        '214' = @(214, 223)      # Inward wrist circles
        '231' = @(231, 685)      # Karate reverse punch
        '253' = @(253, 267)      # Double calf raise
        '256' = @(256, 845)      # Overhead side stretch
        '261' = @(261, 677)      # Bent-elbow reverse fly
        '514' = @(514, 521)      # Symmetric smile / mirror smiling practice
        '755' = @(755, 756)      # Outward wrist circles
    }
}
