@{
    # These are strict identity families, not merely similar exercises. Every
    # member demonstrates the same fundamental movement even when one catalog
    # record is a hold, repetition, or naming variant. A workout may select at
    # most one member of a family as a base muscle-group slot.
    Families = @{
        '1026' = @(1026, 1027) # Teacup arm circles: alternating and simultaneous bilateral demonstrations
        '625' = @(625, 1021) # Segmental standing roll-down: halfway-lift and continuous variants share one identity
        '327' = @(327, 546) # Horizontal elbow strikes: single-side pivoting and alternating planted demonstrations
        '1010' = @(1010, 1018) # Torso circumduction with moving or resting arms
        '712' = @(712, 1012) # Arms-behind chest opening: passive hold and repeated active scapular-squeeze variant
        '437' = @(437, 1002) # Knee-lift under-leg claps: hopping and grounded variants share one identity
        '549' = @(549, 570) # Calf raise with bilateral forward arm elevation; range and fist-close variants
        '31' = @(31, 169, 486) # Same alternating knee lift with two-arm overhead pull-down
        '311' = @(311, 321, 816) # Same low-impact alternating side-tap jack
        '475' = @(475, 919) # Same marching head-turn movement; no independently verified fixed-gaze variant
        '579' = @(579, 580) # Standing back-to-wall shoulder slides and wall-angel range variant
        '266' = @(266, 301) # Standing straight-arm lateral raises through complementary ranges
        '264' = @(264, 275) # Standing shoulder-height arm circles and their small-circle naming variant
        '288' = @(288, 758) # Supported knee-and-ankle circles in opposite directions
        '948' = @(948, 949) # Natural diagonal wood chop, upward and downward naming variants
        '568' = @(568, 633) # Toes-on-wall calf stretch
        '143' = @(143, 538) # Reverse lunge followed by a front kick
        '529' = @(529, 539) # Single-leg hip hinge with rear leg extension
        '211' = @(211, 233) # Straight-elbow assisted wrist-flexion stretch
        '187' = @(187, 252, 253, 254, 562, 566, 581, 582) # Standing calf raises, including neutral and toe-angle variants
        '153' = @(153, 603) # Wall-supported straight-leg hip extension
        '286' = @(286, 541) # Alternating inward forearm blocks

        '292' = @(148, 292, 542) # Same standing bird-dog movement and demonstration
        '550' = @(550, 551) # Squat to calf raise with overhead reach
        '480' = @(480, 517) # Breath of Joy
        '278' = @(278, 326) # Rear-hand straight punch
        '160' = @(160, 533)      # One-leg standing bent-knee hip extension
        '329' = @(329, 531)      # One-arm controlled shoulder circle
        '101' = @(101, 110) # Shoulder-width squat and its shallow pulse variant
        '102' = @(102, 111, 147) # Squat with an overhead arm sweep, including its bent-elbow variation
        '104' = @(19, 103, 104, 105, 107, 109, 136, 199, 626) # Wide turned-out squat, partial/pulse and repetition variants
        '134' = @(134, 137) # Wall-supported squat and its held variant
        '113' = @(113, 135)      # Overhead squat
        '115' = @(115, 532, 996, 997) # Full, partial, and held pistol squat
        '117' = @(116, 117, 123, 150) # Planted lateral squat, side-lunge hold and lateral lunge
        '120' = @(120, 184)      # Split squat
        '124' = @(124, 636)      # Curtsy lunge with side crunch
        '159' = @(159, 649)      # Standing bent-knee hip abduction followed by knee extension
        '192' = @(192, 195) # Lateral lunge followed by a knee-up balance
        '177' = @(177, 186)      # Hip airplane
        '214' = @(214, 223, 236, 755, 756)      # Single-arm wrist controlled circles
        '231' = @(231, 685)      # Karate reverse punch
        '256' = @(256, 845, 958)      # Overhead side stretch
        '270' = @(270, 677) # Goalpost arm movement and hold
        '277' = @(261, 277) # Standing bent-elbow chest squeeze
        '262' = @(262, 507)      # Standing cross-body bicycle crunch

    }
}
