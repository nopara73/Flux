@{
    # Target IDs mapped to a reviewed source whose mechanics are exact when the
    # frame order is reversed. The transform is deterministic and offline.
    222 = @{ Source = 221; ReverseFrames = $true }
    395 = @{ Source = 830; ReverseFrames = $true }
    507 = @{ Source = 508; ReverseFrames = $true }
    610 = @{ Source = 609; ReverseFrames = $true }
    949 = @{ Source = 948; ReverseFrames = $true }
}
