@{
    # Reviewed human sources with deterministic direction transforms that
    # preserve the exact mechanics.
    222 = @{ Source = 221; ReverseFrames = $true }
    949 = @{ Source = 948; ReverseFrames = $true }
}
