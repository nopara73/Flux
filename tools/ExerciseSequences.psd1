# Ordered exercise families that are valuable only when every member is
# completed in one uninterrupted sequence. Each member contributes its own
# reviewed block or side/direction blocks. The first ID is the selectable root;
# every later ID is available only through that root.
@{
    Sequences = @{
        '214' = @(214, 755) # Inward, then outward wrist circles
        '223' = @(223, 756) # Inward, then outward controlled wrist circles
        '288' = @(288, 758) # Forward, then backward knee-and-ankle circles
        '414' = @(414, 418) # Fixed-thumb sides, then alternating-thumb integration
        '617' = @(617, 620) # Forward, then backward standing side-leg circles
    }
}
